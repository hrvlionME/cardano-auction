# Haskell / cabal
dist-newstyle/
*.hi
*.o
*.plutus

# Deno / node
node_modules/
.env

# editors
.DS_Store
*.swp
*.swo
*~

# off-chain run artefacts
off-chain/state/

# The indexer's data now lives in MariaDB, not in the repo. It is derived from
# the chain and rebuildable with `deno task db:reset`, so nothing here is worth
# committing or backing up.
